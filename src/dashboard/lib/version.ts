/**
 * APP_VERSION — single source of truth for the displayed app version.
 * Keep in sync with package.json `version` field.
 */
export const APP_VERSION = '0.1.0'

/**
 * REPO_URL — canonical GitHub repository link shown in the About dialog.
 */
export const REPO_URL = 'https://github.com/ek33450505/cast-desktop'
